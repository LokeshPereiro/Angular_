const name = "Lokesh";

//Accept both number and string
let hpPoints: number | string = 96;

const isAlive: boolean = true;

hpPoints = "Hola";

console.log({ name, hpPoints, isAlive });

export { name, hpPoints, isAlive };
